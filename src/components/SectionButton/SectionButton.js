import React from "react";
import Styled from "./SectionButton.styled";

const SectionButton = () => {
  return (
    <Styled>
      <h2>Here is the Section Button</h2>
    </Styled>
  );
};

export default SectionButton;
